name: Reusable

on:
  workflow_dispatch:

jobs:
  first:
    uses: konradpabjan/reusable-workflow-test/.github/workflows/blank.yml@main
    secrets: inherit
  second:
    uses: konradpabjan/reusable-workflow-test/.github/workflows/second.yml@main
    secrets: inherit
